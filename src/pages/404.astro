---
import StatusHttpError from "@/components/StatusHttpError.astro";
import Layout from "@/layouts/Layout.astro";

import { defaultLang, useTranslations } from "@/hooks/use-translations";

const t = useTranslations(defaultLang);

return Astro.redirect(`/${defaultLang}/404`, 308);
---

<Layout
  title={t("error.http.404.title")}
  description={t("error.http.404.description")}
  image="/profile.jpeg"
>
  <StatusHttpError
    code={404}
    title={t("error.http.404.title")}
    description={t("error.http.404.description")}
  />
</Layout>
